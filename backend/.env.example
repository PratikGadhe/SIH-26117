# Generate a unique value locally, for example: openssl rand -hex 32
COGNIVAULT_JWT_SECRET=
COGNIVAULT_JWT_ALGORITHM=HS256
COGNIVAULT_JWT_EXPIRATION_MINUTES=30

# Optional; defaults to backend/data/cognivault.db
COGNIVAULT_DATABASE_PATH=
